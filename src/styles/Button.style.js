import styled from "styled-components";

const DefaultButton = styled.button`
    outline: none;
    border: none;
    background: none;
    color: inherit;
    font-family: inherit;
    cursor: pointer;
`;

export default DefaultButton;
